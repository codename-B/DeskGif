export const defaultAppSettings = {
  // Processing settings
  functionTimeout: 30000, // 30 seconds in milliseconds
  useGpu: false, // GPU acceleration for FFmpeg

  // Default conversion settings
  defaultVideoCompression: 80,
  defaultFps: 15,
  defaultWidth: 800,
  defaultHeight: 600,

  // Processing defaults
  defaultLoopCount: 0,
  defaultCompressionLevel: 80,
};
